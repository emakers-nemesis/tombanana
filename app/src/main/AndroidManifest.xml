<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emakers.tombanana">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".PraActivity" />
        <activity android:name=".CenActivity" />
        <activity android:name=".DenActivity" />
        <activity android:name=".NpcActivity" />
        <activity android:name=".NfrActivity" />
        <activity android:name=".PspActivity" />
        <activity android:name=".PfrActivity" />
        <activity android:name=".CefActivity" />
        <activity android:name=".DfrActivity" />
        <activity android:name=".NfcActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".sobreActivity"></activity>
    </application>

</manifest>